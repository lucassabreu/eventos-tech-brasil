[tool.pytest.ini_options]
testpaths = ["src/tests"]
addopts = "-q --cov=src --cov-report=term-missing --cov-report=xml --cov-fail-under=85"

[tool.coverage.run]
omit = [
  "src/tests/*",
  "tests/*",
]
